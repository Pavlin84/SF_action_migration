name: test1

on:
  workflow_dispatch:
    inputs: 
      enviroment:
        type: choice
        options:
        - DEV
        - UAT
      schemas:
        type: array
        description: "Target schemas"

jobs:
  task:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Check
        run: |
          echo ${{ github.events.inputs.schemas }}
        
      
